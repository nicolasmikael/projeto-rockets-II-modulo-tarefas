// Converte do formato YYYY-MM-DD para DD/MM/YYYY
// export const formatDateBr = (dateString) => {
//   if (!dateString || !dateString.includes("-")) {
//     console.error("Data inválida detectada (formatDateBr):", dateString);
//   }

//   const [year, month, day] = dateString.split("-");
//   return `${day}/${month}/${year}`;
// };

// // Converte do formato DD/MM/YYYY para YYYY-MM-DD
// export const formatDateToISO = (dateString) => {
//   if (!dateString || !dateString.includes("/")) {
//     console.error("Data inválida detectada (formatDateToISO):", dateString);
//   }

//   const [day, month, year] = dateString.split("/");
//   return `${year}-${month}-${day}`;
// };
